[Назад к README.md](README.md) — [English version](README_4848S040_english.md) *(документация на английском, может отставать от этого файла)*

# ESP32-4848S040 — подключение и прошивка YoRadio

Версия публичной beta: `0.9.434m-r2-lvgl-beta.1`.

## Характеристики платы

| Параметр | Значение |
|---|---|
| MCU | ESP32-S3-WROOM-1-N16R8 (240 MHz, 2 ядра) |
| Модуль | ESP32-S3-WROOM-1-N16R8 |
| Flash | 16 MB |
| PSRAM | 8 MB (Octal) |
| Дисплей | ST7701S, RGB Panel (16-bit) |
| Разрешение | 480×480 |
| Тачскрин | GT911 (I2C) |
| USB/питание | USB-C (данные + питание) |
| Файловая система | LittleFS |

## Что понадобится

- Плата ESP32-4848S040.
- Кабель питания/данных USB-C.
- Внешний I2S DAC или усилитель с I2S-входом (рекомендуется для полноценного звука).
- Провода для подключения ЦАП к заднему разъёму.
- Активная акустика или усилитель на выходе ЦАП.
- Установленный [PlatformIO](https://platformio.org/).

## Важное замечание об аудио

Встроенный моно-ЦАП платы (включается перемычками R21, R22, R23) — не основной рабочий вариант для повседневного прослушивания. Для нормальной эксплуатации YoRadio рекомендуется **внешний I2S DAC или усилитель**, подключаемый к заднему разъёму. Встроенный вариант удобен в первую очередь для быстрой проверки платы без дополнительного оборудования. Схема подключения ЦАП на плате не менялась, поэтому ниже используются уже проверенные фотографии.

## Контакты и подключение

Пины подтверждены по `src/myoptions_4848S040.h`.

| Назначение | GPIO |
|---|---|
| Дисплей: CS (SWSPI) | 39 |
| Дисплей: SCK (SWSPI) | 48 |
| Дисплей: SDA (SWSPI) | 47 |
| Дисплей: DE | 18 |
| Дисплей: VSYNC | 17 |
| Дисплей: HSYNC | 16 |
| Дисплей: PCLK | 21 |
| Дисплей: данные R0–R4, G0–G5, B0–B4 | 16-битная RGB-шина (см. `src/myoptions_4848S040.h`) |
| Подсветка (PWM) | 38 |
| Тачскрин GT911: SDA | 19 |
| Тачскрин GT911: SCL | 45 |
| I2S: DATA (DOUT) | 40 |
| I2S: BCLK | 1 |
| I2S: LRCK / WS (LRC) | 2 |

Пины SD-карты (CS=42, SCK=48, MISO=41, MOSI=47) в этой конфигурации не используются: воспроизведение с SD в публичной beta отключено.

Для внешнего ЦАП нужны линии **BCLK, LRCK/WS, DATA**, а также **GND** и питание — они выведены на задний разъём платы:

![Задний разъём ESP32-4848S040](https://github.com/user-attachments/assets/a40160ec-31af-463c-88bb-d60b51b0e37b)

Перемычки встроенного моно-ЦАП (R21, R22, R23):

![Перемычки ESP32-4848S040](https://github.com/user-attachments/assets/d2fbc24c-9036-4c09-ace8-5323dc4e30fb)

Задняя сторона платы:

![Задняя сторона платы ESP32-4848S040](https://github.com/user-attachments/assets/34dd64a3-305e-4486-a07c-fedf7416baa0)

Точные электрические детали конкретной ревизии платы уточняйте по её маркировке и схеме.

## Подготовка конфигурации

1. Скопируйте `src/myoptions_4848S040.h` поверх `src/myoptions.h`.
2. В `src/myoptions.h` задайте язык интерфейса — `L10N_LANGUAGE`: `RU`, `EN` или `PL`.
3. В PlatformIO выберите окружение `[env:4848S040]` из `platformio.ini`.
4. Данные Wi-Fi и список станций не хранятся в исходниках — они настраиваются при первом запуске (Wi-Fi Setup) и через Web UI/файлы в `data/`.

## Сборка

```bash
pio run -e 4848S040 -t clean
pio run -e 4848S040
pio run -e 4848S040 -t buildfs
```

Перед сборкой из исходников обязательно замените системные библиотеки ESP-IDF — см. раздел ниже.

Для прошивки:

```bash
pio run -e 4848S040 -t upload
pio run -e 4848S040 -t uploadfs
```

`uploadfs` перезаписывает весь раздел LittleFS содержимым `data/` — пользовательские файлы на устройстве, которых нет в `data/`, будут потеряны.

### Обязательные библиотеки ESP-IDF

Эта установка относится только к сборке из исходников. Для текущей beta используется полный KnownGood-набор:

```text
library!/esp32s3_5_5_2__3_3_6_ai_tls_profile_b_FULL_WORKING_20260603_221741/
```

Он собран для PIOArduino `55.03.36`, Arduino-ESP32 `3.3.6` и пакета `framework-arduinoespressif32-libs` `5.5.0+sha.f56bea3d1f` (ESP-IDF 5.5.2). Нельзя брать эти `.a` из другого выпуска Arduino или ESP-IDF.

Скопируйте с заменой все 10 архивов из этого каталога:

```text
libesp_http_client.a
libesp_netif.a
libesp-tls.a
libhttp_parser.a
liblwip.a
libmbedcrypto.a
libmbedtls.a
libmbedtls_2.a
libmbedx509.a
libtcp_transport.a
```

Целевой каталог:

- Windows: `%USERPROFILE%\.platformio\packages\framework-arduinoespressif32-libs\esp32s3\lib\`
- Linux/macOS: `~/.platformio/packages/framework-arduinoespressif32-libs/esp32s3/lib/`

Штатные архивы framework package заменяются намеренно: `liblwip.a` и `libesp_netif.a` содержат сетевой профиль для стабильной работы потоков, а согласованный mbedTLS/TLS-набор, включая `libmbedtls_2.a`, нужен для AI HTTPS. Ранний каталог `library!/esp32s3_5_5_2__3_3_6/` содержит только LwIP/netif и не является полным комплектом текущей beta.

После копирования перезапустите PlatformIO, выполните clean и соберите `4848S040` заново. Переустановка или обновление `framework-arduinoespressif32-libs` возвращает штатные архивы, поэтому после неё замену нужно повторить.

## Готовые бинарники

Готовые прошивки размещаются в каталоге [`build_bin/4848S040/`](build_bin/4848S040/) в трёх языковых вариантах — `RU`, `EN`, `PL`. Инструкция по адресам прошивки и порядку записи — в [`build_bin/4848S040/README.md`](build_bin/4848S040/README.md).

> На момент публикации этого README файлы `.bin` ещё не добавлены — каталог содержит только инструкции. Появятся отдельным release-этапом.

## Первый запуск

После включения YoRadio показывает загрузочный экран и пытается подключиться к сохранённой сети Wi-Fi. Если сеть ещё не настроена или недоступна, открывается Wi-Fi Setup / Recovery: список найденных сетей (включая ранее сохранённые), выбор сети, ввод пароля (для открытой сети пароль не запрашивается) и сохранение. После сохранения устройство перезагружается и открывает Main.

Если Recovery остаётся без действий, YoRadio автоматически поднимает точку доступа `yoRadioAP` (без пароля); её также можно открыть вручную. Подключитесь к ней и продолжите настройку через Web UI по IP-адресу точки доступа.

## Управление на экране

| Жест | Действие |
|---|---|
| Горизонтальный свайп | Переключение между Info / Main / Visual / Station / Weather / Settings |
| Слайд по громкости на Main | Регулировка громкости |
| Вертикальный свайп на Station | Листание списка станций |
| Свайп вниз от верхнего края (Info/Main/Visual/Weather) | Открыть Preset Temporary |
| Короткое / долгое нажатие в Preset | Запустить сохранённую станцию / сохранить текущую в слот |
| Касание экрана в Screensaver | Выход из заставки |

## Основные страницы

### Main

Главный экран воспроизведения. Логотип/обложка станции, название станции и трека, панель управления и громкость.

![Экран Main](readme/main-screen-guide.png)

### Info

Диагностика: сеть, IP, прошивка, чип, дисплей, память. Полезно при поддержке и отладке подключения.

![Экран Info](readme/info-screen-guide.png)

### Visual

VU-индикатор в стиле винтажных приборов Beocord, работает только во время воспроизведения потока.

![Экран Visual](readme/visual-screen-guide.png)

### Station

Список станций с текущей позицией и индикатором активного воспроизведения.

![Экран Station](readme/station-screen-guide.png)

### Weather

Текущая погода и прогноз; данные обновляются с сервера OpenWeatherMap.

![Экран Weather](readme/weather-screen-guide.png)

### Settings

Быстрые настройки устройства и переход в подробные разделы (Display, Wi-Fi).

![Экран Settings](readme/settings-screen-guide.png)

## Settings

Экран Settings — точка входа в следующие разделы:

- **Display details** — яркость, Auto Dim (задержка и целевой уровень), выбор темы.
- **Sleep Timer** — таймер, по истечении которого выполняется выбранное действие.
- **Sleep Device** — действие Sleep Timer, переводящее устройство в глубокий сон (deep sleep).
- **Wi-Fi** — переход к настройке текущей сети.

![Настройки Display](readme/display-settings-guide.png)

![Режим Sleep Device](readme/sleep-device-guide.png)

## Screensaver

Аналоговые часы на весь экран в режиме ожидания. Включение/отключение и оформление настраиваются через Web UI. Выход — касание экрана, после чего устройство возвращается к воспроизведению.

![Экран Screensaver](readme/screensaver-guide.png)

## Web UI

IP-адрес устройства можно посмотреть на экране Info. Откройте `http://<ip-адрес-устройства>/` в браузере.

![Web UI, иконка Appearance](readme/webui-appearance-entry.png)

- **Player / Settings** — управление воспроизведением и поведением устройства.
- **Appearance** — station artwork, Color Theme / Custom Palette, Main Screen Backgrounds (подробности и скриншоты — в [основном README](README.md#appearance)).
- **/update** — отдельное обновление основной прошивки (`firmware.bin`) или образа LittleFS (`littlefs.bin`).

Сохранность пользовательских настроек и плейлиста зависит от выбранной операции обновления; полностью стирать Flash без необходимости не требуется.

## Ограничения и диагностика

- Public beta поддерживает только ESP32-4848S040.
- Воспроизведение с SD-карты отключено.
- Язык интерфейса выбирается при сборке и не переключается во время работы.
- Выход из Sleep Device (deep sleep) — кнопкой Reset или перезапуском питания; пробуждение касанием не поддерживается.

| Симптом | Что проверить |
|---|---|
| Экран не запускается | Питание и USB-кабель, окружение `4848S040`, конфигурацию `src/myoptions.h`. |
| Wi-Fi не подключается | Сеть 2.4 ГГц, пароль, повторное сканирование или настройку через открытую точку `yoRadioAP`. |
| Web UI не открывается | Актуальный IP-адрес на экране Info, общую сеть с устройством и его доступность. |
| Звук отсутствует | Выбранный вариант аудиотракта (внешний ЦАП или перемычки встроенного), громкость и доступность потока станции. |
| Внешний ЦАП не работает | Подключение BCLK/LRCK/DATA/GND согласно таблице пинов и фото заднего разъёма. |
| Файловая система не прошита | Выполните `uploadfs` / полную прошивку LittleFS по адресу из инструкции `build_bin`. |
| Фон или artwork не применяется | Формат и размер файла (480×480 для фона, автоматическая подготовка 120×120 для artwork), повторите загрузку через Appearance. |
| После Sleep Device устройство не реагирует на касание | Это штатное поведение deep sleep — нажмите Reset или переподключите питание. |
| Неверный язык интерфейса | Проверьте `L10N_LANGUAGE` в `src/myoptions.h` и пересоберите прошивку. |

Если не показывается погода — проверьте сеть и раздел Weather/API в Web UI. Если AI Layer молчит, это может быть штатным поведением; для проверки убедитесь, что заданы API, модель и prompt.

## Безопасность

- Web UI работает в локальной сети устройства и не использует HTTPS.
- Не публикуйте API-ключи (Weather, AI) в общедоступных местах.
- Не храните и не публикуйте пароли Wi-Fi вашей сети вместе с конфигурацией проекта.

## Завершение

- Основной README проекта → [README.md](README.md).
- Вопросы и ошибки → [Issues](https://github.com/Witaliy76/Yoradio_RGB_Panel/issues).
- Автор адаптации: [Witaliy76](https://github.com/Witaliy76).
- Лицензия: GNU General Public License v3 or later — см. [`LICENSE`](LICENSE).
